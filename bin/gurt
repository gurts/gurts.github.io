#!/bin/bash

if [ $# -lt 1 ]; then
  echo "Usage: $0 "
  exit
fi

BASE_DIR="$( cd "$( dirname "$0" )/.." && pwd )"

case "$1" in

serve)
  SOURCE_DIR="$BASE_DIR/src"
  DESTINATION_DIR="$BASE_DIR/tmp/build"
  jekyll serve --source "$SOURCE_DIR" --destination "$DESTINATION_DIR"
  ;;

esac


